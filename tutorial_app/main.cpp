#include <iostream>
#include <cstdlib>

int main() {
  std::string line;
  system("clear");

  std::cout << "OS自作はとてもニッチな趣味ですが, \n";
  std::cout << "時々やっている人を見かけます. \n";
  std::cout << "「OSを自作してみよう」とおもうまでには, \n";
  std::cout << "まずコンピュータに触れる経験が必要で, \n";
  std::cout << "加えてOSの存在に気づく必要があるはずです. \n";
  std::cout << std::endl;

  std::cout << "エンターを入力してください: ";
  std::getline(std::cin, line);
  system("clear");

  std::cout << "業務命令でOSを自作するようなケースを除けば, \n";
  std::cout << "OSの存在に気づいた上で, さらにOSに興味を持つ必要があります. \n";
  std::cout << "彼ら/彼女らはなぜ「OSの自作」に興味を持つのかを調査してみました. \n";
  std::cout << std::endl;

  std::cout << "エンターを入力してください: ";
  std::getline(std::cin, line);
  system("clear");

  std::cout << "大掃除の時にWindows 95 ファーストステップガイドを見つけ, \n";
  std::cout << "その後Windows95のCD-ROMが手に入ったので Windows95を使ってみた. \n";
  std::cout << "Windows 95 が出すエラーメッセージ (EAX のレジスタ値などが表示されていた)\n";
  std::cout << "のを見てOSの仕組みに興味が出てきて, その後OSを作ることを思いついた. \n";
  std::cout << std::endl;

  std::cout << "エンターを入力してください: ";
  std::getline(std::cin, line);
  system("clear");

  std::cout << "BASICより速いプログラムを作るために機械語を学んでいたら, \n";
  std::cout << "BASICより汎用的なプログラミング環境であるOSというものの存在を知った. \n";
  std::cout << std::endl;

  std::cout << "エンターを入力してください: ";
  std::getline(std::cin, line);
  system("clear");

  std::cout << "学生の頃はOSというものを認識しないままWindowsを使っていたが, \n";
  std::cout << "社会人になり様々なOSを使うようになるとOSの存在に気づいた. \n";
  std::cout << std::endl;

  std::cout << "エンターを入力してください: ";
  std::getline(std::cin, line);
  system("clear");
}